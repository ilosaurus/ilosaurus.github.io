backend:
  name: github
  repo: ilosaurus/ilosaurus.github.io # Ganti dengan username/repo Anda
  branch: main # Ganti dengan branch utama Anda
  client_id: Ov23liSZve0adb0yjMEX # Client ID GitHub OAuth Anda

media_folder: "static/images" # Folder tempat gambar diunggah
public_folder: "/images" # Path publik untuk gambar

collections:
  - name: "posts"
    label: "Posts"
    folder: "content/posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Draft", name: "draft", widget: "boolean", default: false}
      - {label: "Categories", name: "categories", widget: "list", default: ["Uncategorized"]}
      - {label: "Tags", name: "tags", widget: "list", default: []}
      - {label: "Description", name: "description", widget: "string", required: false}
      - {label: "Body", name: "body", widget: "markdown"}