# Nama workflow
name: Deploy Hugo site to Pages

on:
  # Jalankan saat ada push ke branch `main`
  push:
    branches:
      - main
  # Memungkinkan Anda menjalankan workflow ini secara manual dari tab Actions
  workflow_dispatch:

# Izin untuk GITHUB_TOKEN agar bisa men-deploy ke GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Izinkan hanya satu deployment berjalan pada satu waktu
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4 # DIUPDATE dari v3
        with:
          submodules: true  # Ambil juga tema (git submodule)

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3 # DIUPDATE dari v2
        with:
          hugo-version: 'latest'
          extended: true

      - name: Build
        run: hugo --minify

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3 # DIUPDATE dari v1

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4 # DIUPDATE dari v1